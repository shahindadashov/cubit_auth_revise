import 'package:cubit_auth_revise/features/splash/presentation/splash_page.dart';
import 'package:flutter/material.dart';

class CubitRevise extends StatelessWidget {
  const CubitRevise({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(home: SplashPage());
  }
}
